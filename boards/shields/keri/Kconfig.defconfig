if SHIELD_KERI_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "keri"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_KERI_LEFT || SHIELD_KERI_RIGHT

config ZMK_SPLIT
    default y

endif